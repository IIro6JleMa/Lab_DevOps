events {}
http {
  server {
    listen 80;
    location / {
      return 200 'Hello DevOps Lab\n';
      add_header Content-Type text/plain;
    }
    location /api/ {
      proxy_pass http://api:8000/;
    }
  }
}
